<Window x:Class="DESKTOP_APP.Views.DriverView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DESKTOP_APP.Views"
        mc:Ignorable="d"
        Title="DriverView" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBox x:Name="driverLastNameTextBox"/>
            <TextBox x:Name="driverFirstNameTextBox"/>
            <TextBox x:Name="driverMiddleNameTextBox"/>
            <TextBox x:Name="driverPassportTextBox"/>
            <TextBox x:Name="driverRegAddressTextBox"/>
            <TextBox x:Name="driverLiveAddressTextBox"/>
            <TextBox x:Name="driverPlaceOfWorkTextBox"/>
            <TextBox x:Name="driverWorkPositionTextBox"/>
            <TextBox x:Name="driverEmailTextBox"/>
            <TextBox x:Name="driverRemarksTextBox"/>
            <Button x:Name="showImageButton" Content="Показать изображение" Click="showImageButton_Click"/>
            <Button x:Name="saveButton" Content="Сохранить" Click="saveButton_Click"/>
        </StackPanel>
        
    </Grid>
</Window>
