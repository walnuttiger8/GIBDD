<Window x:Class="DESKTOP_APP.Views.NavigationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DESKTOP_APP.Views"
        mc:Ignorable="d"
        Title="Главное меню" Height="450" Width="800" ResizeMode="NoResize" Background="LightGray">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="20"/>
            <Setter Property="Width" Value="300"/>
            <Setter Property="Height" Value="40"/>
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel Margin="100 10 100 5">
            <Image Source="../Resources/gibdd_logo.png" Height="90"/>
            <Button x:Name="driversButton" Content="Водители" Click="driversButton_Click"/>
            <Button x:Name="vehiclesButton" Content="Транспортные средства" Click="vehiclesButton_Click"/>
            <Button x:Name="finesButton" Content="Штрафы"/>
        </StackPanel>
    </Grid>
</Window>
